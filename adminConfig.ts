// ============================================================================
// ADMIN AUTH CONFIGURATION (DEMO ONLY)
// ----------------------------------------------------------------------------
// This project ships as a frontend-only demo. For production use, REMOVE this
// file entirely and replace `src/context/AuthContext.tsx` with real calls to
// a backend (e.g. Firebase Auth, Supabase Auth, or your own API using JWT /
// sessions). Never ship real admin credentials inside frontend source code.
// ============================================================================

export const DEMO_ADMIN_CREDENTIALS = {
  username: "admin",
  password: "arentertainment2026",
};

export const AUTH_STORAGE_KEY = "ar_ent_admin_session";
